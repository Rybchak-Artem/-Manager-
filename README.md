# -Manager-
The code uses LLM to parse the entered text for the desired characteristics and enters them into the database

# How to start

add .env file with:
1. DB_URL=UR_DB
2. GROQ_API_KEY=UR_KEY

download modules `npm i`

`node /app.js` to start 
